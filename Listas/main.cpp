#include <iostream>
#include "Lista.h"

using namespace std;

int main()
{
    Lista l;
    l.agregar(10);
    l.agregar(20);
    l.agregar(30);
    l.agregar(40);
    l.imprimir();
    return 0;
}
